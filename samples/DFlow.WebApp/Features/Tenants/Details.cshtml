@model DFlow.WebApp.Features.Tenants.TenantViewModel

@{
    ViewData["Title"] = Model.Title;

    Func<dynamic, object> Links =

        @<div class="links">
             <a asp-action="Index" asp-all-route-data="@Model.LastIndexRouteDictionary">Índice</a> |
             <a asp-action="Create">Agregar Cliente</a> |
            <a asp-action="Edit" asp-route-id="@Model.Id">Modificar</a> |
            <a asp-action="Delete" asp-route-id="@Model.Id">Eliminar</a>
        </div>;

}

<h2>@Model.Title</h2>

@Links(Model)

@Html.Partial("AlertsPartial")

<h3 style="color: darkblue">@Model.Owner</h3>

<div class="row">
    <div class="col-sm-9">
        <form>
            <div asp-validation-summary="All" class="text-danger"></div>

            @Html.Partial("_DisplayPartial", Model)

            <hr />

        </form>
    </div>

    <div class="col-sm-3">
    </div>

</div>

@Links(Model)