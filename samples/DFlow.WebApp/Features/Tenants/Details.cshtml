@model DFlow.WebApp.Features.Tenants.TenantViewModel

@{
    var title = "Consultar Cliente";

    ViewData["Title"] = title;
}

<h2>@title</h2>

@Html.Partial("AlertsPartial")

<div class="row">
    <div class="col-sm-9">
        <div asp-validation-summary="All" class="text-danger"></div>

        @Html.Partial("_DisplayPartial", Model)

    </div>
    <div class="col-sm-3">

    </div>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index" asp-all-route-data="@Model.ReturnToIndexRouteValues">Back to List</a>
</div>
